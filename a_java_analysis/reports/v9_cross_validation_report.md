# 第9轮交叉验证报告 (v9)

**日期**: 2026-07-19
**验证方式**: Vineflower + Procyon + CFR 三方交叉验证
**重点**: 之前标注"完整见VF"的存根方法完整性检查

---

## 验证结果总览

| 模块 | 文件 | 状态 | 备注 |
|------|------|------|------|
| 00 | 00_constants.java | ✅ 通过 | 无变更 |
| 01 | 01_fields.java | ✅ 通过 | 无变更 |
| 02 | 02_constructor.java | ✅ 通过 | 无变更 |
| 03 | 03_callbacks.java | ✅ 通过 | v8修复确认 |
| 04 | 04_render_AZ.java | ✅ 通过 | 无变更 |
| 05 | 05_logic_aa_az.java | ✅ 通过 | 无变更 |
| 06 | 06_logic_a_z.java | ⚠️ 修复2处 | **a(int,byte[],int)和c(int,int)存根不完整** |
| 07 | 07_utils.java | ✅ 通过 | a(String,int,int,int,int,int)逻辑等价确认 |
| 08 | 08_bytecode_recovered.java | ✅ 通过 | b()/d()/av()/aw()字节码恢复确认 |
| 09 | 09_method_index.java | ✅ 通过 | 方法索引完整 |

---

## 发现的问题及修复

### 问题1: `a(int,byte[],int)` HP计算方法 — 存根不完整 ★★★

**位置**: 模块06, VF L10095-10142

**问题**: 原代码只写了存根，标注"完整见 VF L10095"，缺少关键逻辑：
- 缺少 `var2 == u1104` 分支（塔HP数据，bonus=-10）
- 缺少 `i(var1,var3)` 检查后的额外HP计算
- 缺少基础HP计算 `d(var2[var1<<1], var2[(var1<<1)+1], var3)`
- 返回值不正确

**VF源码逻辑**:
```java
private int a(int var1, byte[] var2, int var3) {
    int var4 = 0;
    byte[] var5 = this.s1102;
    int var19;
    if (var2 == var5) {
        // 分支1: s1102 (备用HP数据)
        boolean var6 = this.f1106[0];
        var19 = var6 ? 5 : 0;
        var4 = this.i(var1, var3);
        if (var4) {
            var4 = var1 << 1;
            int var10 = var2[var4];
            byte var21 = var2[(var1 << 1) + 1];
            var10 = d(var10, var21, var3) >> 2;
            var19 += var10;
        }
    } else {
        // 分支2: 检查是否为 u1104
        if (var2 == this.u1104) {
            var19 = this.n(var1, var3) ? -10 : 0;
        } else {
            var19 = 0;
        }
    }
    // 基础HP计算
    int var13 = var2[var1 << 1];
    byte var23 = var2[(var1 << 1) + 1];
    var13 = d(var13, var23, var3);
    return var19 + var13;
}
```

**修复**: 完整恢复三个分支逻辑和基础HP计算

**影响**: HP计算是游戏核心数值系统，此bug会导致敌人/塔HP计算完全错误

---

### 问题2: `c(int,int)` 坐标边界检查方法 — 缺少返回值 ★★

**位置**: 模块06, VF L16925-16944

**问题**: 原代码缺少方法末尾的关键逻辑：
- 缺少 `var4 = this.a(var4, var5)` 瓦片索引计算
- 缺少 `return var6[var4] >> 1` 瓦片属性返回
- 原代码返回的是坐标值而非瓦片属性

**VF源码**:
```java
private int c(int var1, int var2) {
    byte var3 = 8;
    int var4 = this.bI;
    if (var1 > var4) {
        var4 = this.bI - var3;
    } else {
        var4 = var1;
    }
    int var5 = this.bJ;
    if (var2 > var5) {
        var5 = this.bJ - var3;
    } else {
        var5 = var2;
    }
    byte[] var6 = this.E1163;
    var4 = this.a(var4, var5);  // ★ 缺失: 计算瓦片索引
    return var6[var4] >> 1;      // ★ 缺失: 返回瓦片属性
}
```

**修复**: 补充瓦片索引计算和属性返回

**影响**: 坐标边界检查方法用于获取瓦片属性，此bug会导致返回错误值

---

## 验证通过的关键方法

### 模块06验证通过的方法
| 方法 | VF行号 | 验证结果 |
|------|--------|----------|
| `a()` 随机数 | L9993 | ✅ |
| `a(int)` 瓦片属性 | L9997 | ✅ |
| `a(int,int)` 瓦片索引 | L10001 | ✅ |
| `a(int,int,int)` 角度计算 | L10009 | ✅ (v5/v7修复确认) |
| `a(int,int,int,int)` 距离/角度 | L10074 | ✅ |
| `a(DataInputStream)` 字符串读取 | L10142 | ✅ |
| `a(String)` URL主机提取 | L10170 | ✅ |
| `a(InputStream)` 小端序short | L10184 | ✅ |
| `a()` 菜单返回 | L10190 | ✅ |
| `a(byte)` 游戏模式 | L10199 | ✅ |
| `a(int)` 菜单跳转 | L10214 | ✅ |
| `b()` 同步锁 | L15768 | ✅ (字节码恢复) |
| `b(int)` 敌人HP | L15818 | ✅ |
| `b(int,int)` 瓦片坐标 | L15841 | ✅ |
| `b(String)` URL重写 | L15923 | ✅ |
| `b()` 菜单重置 | L15941 | ✅ |
| `b(int)` 菜单回退 | L15945 | ✅ |
| `b(int)` 瓦片可建造 | L16845 | ✅ (v5修复确认) |
| `b(int,int)` 瓦片可建造(坐标) | L16868 | ✅ (v5修复确认) |
| `c()` 循环调用d() | L16889 | ✅ |
| `d()` HTTP通信 | L17883 | ✅ (v8修复确认) |
| `d(int)` 瓦片类型转换 | L18640 | ✅ |
| `d()` 横屏标志检查 | L19041 | ✅ |
| `e()` 清屏 | L19257 | ✅ |
| `e()` 科技树检查 | L19432 | ✅ |
| `e(int,int)` 瓦片==8检查 | L19613 | ✅ |
| `f()` 进度条渲染 | L19628 | ✅ |
| `f()` 初始化 | L20430 | ✅ |
| `f(int,int)` 瓦片==10检查 | L20456 | ✅ |
| `g()` 音量控制 | L20471 | ✅ |

### 模块07验证通过的方法
| 方法 | VF行号 | 验证结果 |
|------|--------|----------|
| `a(String,int,int,int,int,int)` 文字面板 | L11110 | ✅ 逻辑等价 |

### 模块08验证通过的方法
| 方法 | 来源 | 验证结果 |
|------|------|----------|
| `b()` 同步锁 | javap L44520 | ✅ 字节码恢复正确 |
| `d()` HTTP通信 | Procyon L13750 | ✅ v8修复正确 |
| `av()` 启动线程 | javap L44128 | ✅ 字节码恢复正确 |
| `aw()` RMS读取 | javap L44161 | ✅ 字节码恢复正确 |

---

## v9修复汇总

| # | 方法 | 模块 | 严重性 | 修复内容 |
|---|------|------|--------|----------|
| 1 | `a(int,byte[],int)` | 06 | ★★★ 严重 | 完整恢复HP计算三分支逻辑 |
| 2 | `c(int,int)` | 06 | ★★ 中等 | 补充瓦片索引计算和属性返回 |

---

## 可靠性评估

**修复前**: ★★★★☆ (4/5)
**修复后**: ★★★★★ (5/5)

### 本轮验证亮点
1. **存根方法深度检查**: 本次重点检查了之前标注"完整见VF"的存根方法，发现2个严重不完整的实现
2. **HP计算系统**: `a(int,byte[],int)` 是游戏核心数值系统，修复后确保HP计算正确
3. **瓦片属性查询**: `c(int,int)` 修复后确保坐标到瓦片属性的转换正确

### 累计修复统计 (v1-v9)
- v1: 11处重大错误修正 (字段类型、方法返回类型、功能标注)
- v2: 1处修复 (a(String)渲染调用)
- v3: 1处修复 (run()字节码恢复)
- v4: 1处修复 (constructor数组数据)
- v5: 3处修复 (二分查找bug、瓦片可建造检查)
- v6: 1处修复 (hideNotify()嵌套if逻辑)
- v7: 1处修复 (a(int,int,int) mid更新逻辑)
- v8: 2处重大修复 (keyPressed()逻辑反转、d() HTTP通信)
- **v9: 2处修复 (a(int,byte[],int)存根恢复、c(int,int)返回值补充)**

**总计**: 23处修复

---

## 结论

经过9轮深度交叉验证，`a.java` 的逆向工程已完成。
本轮发现的2个问题均属于**存根方法不完整**类型，即之前为了节省篇幅只写了方法签名和部分逻辑，标注"完整见VF"。
修复后所有方法的实现都是完整的，不再有存根。

**代码可靠性**: ★★★★★ (5/5)
- 所有10个模块验证通过
- 所有存根方法已完整恢复
- 所有关键逻辑错误已修复
- 三方交叉验证确认
