{
  "name": "weicheng-wushuang-h5",
  "version": "1.0.0",
  "description": "危城无双之三国群英 H5还原版 - 100%还原J2ME原版",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "esbuild tests/timing.test.ts --bundle --platform=node --format=cjs --outfile=.test-dist/timing.test.cjs && node --test .test-dist/timing.test.cjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "midi-player-js": "^2.0.14",
    "soundfont-player": "^0.12.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
